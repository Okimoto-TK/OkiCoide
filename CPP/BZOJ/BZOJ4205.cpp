#include <cstdio>
#include <cstring>
#define N 30000
int head[N * 10 + 5];
struct Edge
{
	int u, v, nxt, f;
} e[N * 100 + 5];
int n, m, pool;
int main()
{
	scanf("%d%d", &n, &m);
	for(int i = 2; i <= )
}
